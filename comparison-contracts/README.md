# Comparison contracts

No provider comparison ships in v0.1.0-preview.1. A verdict is forbidden until a provider-specific contract validates against `contract.schema.json` and aligns protocol, population, sequence, conversion type, attribution window, time axis, timezone, and finality. Until then, sources remain side-by-side with `comparability: none`.
