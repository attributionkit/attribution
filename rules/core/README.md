# Core rule bundle v0.1.0-preview.1

The Go CLI implements the versioned local rules. Each result retains execution, verdict, evidence, basis, integrity, and comparability independently.

The current bundle covers desired-state validity, conversion authority conflicts, Expo plugin registration, native module installation, Apple endpoint wiring, SKAdNetwork configuration, Meta public-id wiring, Meta conversion-management disabling, client secret scanning, generated-plan hash drift, strict expiring simulator-report import under `runtime.report-imported`, and honest device/production pending states.
